# Env Diff Report

Comparing `.env.example` and `.env`.

## Missing in `.env` (present in `.env.example`)

- `NEW_FEATURE_FLAG`
- `SENTRY_DSN`

## Missing in `.env.example` (present in `.env`)

- `LOCAL_ONLY_FLAG`

## Present in both (4)

- `API_KEY`
- `DATABASE_URL`
- `DEBUG`
- `REDIS_URL`
